import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  loader: {
    margin: 0,
    position: 'absolute',
    top: '50%',
    left: '50%',
    height: '70px',
    marginLeft: '-35px',
    marginTop: '-35px'
  }
}));
